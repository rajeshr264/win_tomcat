---
win_tomcat::conffile: "c:\tools\${win_tomcat::version}"
win_tomcat::version: 'installed' # "installed" installs the default version, otherwise specify an approved version
win_tomcat::install_location: 'c:\\tools' #double backslash for chocolatey, since it will escape the rest
win_tomcat::major_version: "8" # I want to get the major version of tomcat, basically the first number in the apache_version